pensando em uma lista com n elementos inteiros que n estao ordenados e podem se repetir, e alguma comhinacao deles deve somar um numero P,  qual seria uma opção de se fazer isso? Um hash seria eficiente, porém eu teria que mapear a array, obtendo o maior número, considerando que são todos inteiros, então eu faria o seguinte:
Esse hash vai de 0 até max(array), esse hash em cada id possuiria uma array de ints, ou seja é um hash de ponteiros de inteiro que por padrão, apontam para NULL, essa array de inteiros possui a posição(id) na array original fos elementos que se repetem, o resto é só fazer:
Aux = Target-(maiorNumeroNoHash ou max);
Se aux existe no hash, pega o id do primeiro e salva num hash de posicoes que já verificadas. E continua assim até que:
Aux = 0; se percorrer tudo e não houver aux = 0, não há combinação. E temos O(n²)